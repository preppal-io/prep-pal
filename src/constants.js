// Stock thresholds (percentage)
export const LOW_STOCK_THRESHOLD = 65
export const CRITICAL_STOCK_THRESHOLD = 35

// UI timeouts (milliseconds)
export const NOTIFICATION_TIMEOUT_MS = 3000

// Supported locales
export const SUPPORTED_LOCALES = ['fr_CH', 'de_CH', 'en_US']

// Storage keys (for browser localStorage)
export const STORAGE_KEYS = {
  PRODUCT_CATEGORIES: 'productCategories',
  STOCK: 'stock',
  USER_PROFILE: 'userProfile',
}
